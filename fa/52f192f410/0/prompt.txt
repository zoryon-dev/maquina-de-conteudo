/pr-review-toolkit:review-pr

---

Prefiro que faça o seguinte: 

Lance agente em paralelo (sub-agent/seção) para corrigir TODAS as ações. TODAS. 

Em paralelo aqui, seguimos para a próxima fase (PR2)

---

/pr-review-toolkit:review-pr revise o pr2

---

positivo

---

<task-notification>
<task-id>ad626706b62f67bdf</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-jonassilva-Documents-developer-maquina-de-conteudo/3877d24a-7307-489b-9482-0f0573432eda/tasks/ad626706b62f67bdf.output</output-file>
<status>completed</status>
<summary>Agent "PR2-FIX-A: injection.ts" completed</summary>
<result>Nenhuma referência em qualquer lugar do projeto. Tudo limpo.

---

## Relatório

**Arquivo**: `/Users/jonassilva/Documents/developer/maquina-de-conteudo/src/lib/brands/injection.ts`

**Tamanho**: 156 linhas → 131 linhas (diminuiu **25 linhas**, ~16%).

**Fixes aplicados**:

1. **Separator bug (crítico)**: `buildAudienceFears` e `buildAudienceDesires` agora usam `", "` em vez de `"; "`, consistente com o parser `splitCsv` do consumer. Confirmado — busca no arquivo não retorna mais nenhum `.join("; ")` (o único remanescente `beliefs.join("; ")` dentro de `buildDifferentiators` é intencional, é embutido numa string rotulada, não consumido como CSV).

2. **Dead code**: Função `mergeBrandWithUserVars` e seu comentário decorativo prévio deletados. Grep global confirma zero referências no repo.

3. **Threshold**: Constante `TOM_ATRIBUTO_THRESHOLD = 60` extraída no topo do arquivo (após imports) e aplicada nas 5 comparações em `buildTone`.

4. **Helper `joinIfAny`**:
   ```ts
   function joinIfAny(parts: Array&lt;string | null | undefined&gt;, sep = " | "): string | undefined {
     const filtered = parts.filter((s): s is string =&gt; !!s &amp;&amp; s.trim().length &gt; 0)
     return filtered.length &gt; 0 ? filtered.join(sep) : undefined
   }
   ```
   Aplicado em: `buildTone`, `buildTargetAudience` (uso interno + externo com `" — "` e `" | "`), `buildAudienceDesires`, `buildDifferentiators` (com `" ∥ "`), `buildContentGoals`. `buildNegativeTerms` e `buildPreferredCTAs` mantidos (patterns diferentes).

5. **Comentário decorativo removido**: linha `// ── builders (exportados para uso avançado...) ──` deletada. Comentários WHY importantes preservados (header, acoplamento, formato CSV).

**Validação**: `npx tsc --noEmit --skipLibCheck | grep injection` retornou vazio. Zero referências quebradas a `mergeBrandWithUserVars` em todo o repo — nada pra coordenar.</result>
<usage><total_tokens>37044</total_tokens><tool_uses>5</tool_uses><duration_ms>69588</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>abd5fdde9e8cf3137</task-id>
<tool-use-id>toolu_01J8H4G8ncZaYPJw1e1vFVbH</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-jonassilva-Documents-developer-maquina-de-conteudo/3877d24a-7307-489b-9482-0f0573432eda/tasks/abd5fdde9e8cf3137.output</output-file>
<status>completed</status>
<summary>Agent "PR2-FIX-B: errors + precedência + splitCsv" completed</summary>
<result>Both pass clean: no type errors on the three touched files, and 30/30 tests green.

---

## Report

**Arquivos tocados:**
- `/Users/jonassilva/Documents/developer/maquina-de-conteudo/src/lib/wizard-services/llm.service.ts`
- `/Users/jonassilva/Documents/developer/maquina-de-conteudo/src/lib/wizard-services/user-variables.service.ts`
- `/Users/jonassilva/Documents/developer/maquina-de-conteudo/src/app/api/wizard/[id]/generate-titles/route.ts`

**Fix 1 — ConfigError propagation:** Importei `isAppError` de `@/lib/errors` nos dois arquivos. O `catch` interno do `getBrandPromptVariables()` agora faz `if (isAppError(err) &amp;&amp; (err.code === "CONFIG_ERROR" || err.code === "NOT_FOUND")) throw err` antes de cair no fallback silencioso. Uso `isAppError` + checagem de `err.code` porque `errors.ts` expõe `AppError` como base comum com campo `code: string` (`ConfigError` -&gt; `"CONFIG_ERROR"`, `NotFoundError` -&gt; `"NOT_FOUND"`). Na route, o re-throw acontece dentro do `.catch()` do `Promise.all` e também no `catch` externo do `try/catch` envolvente (dupla guarda), pra garantir que o erro vire 500 com contexto ao invés de ser engolido.

**Fix 2 — brandVoice:** Removi `userVars.brandVoice ||` da expressão `voiceTone` na route. Justificativa documentada em comentário: `brandVoice` é descrição da voz da marca (campo separado), não é tom. Precedência agora idêntica ao `llm.service`: `userVars.tone || brandVars.tone` (sem input nessa rota específica).

**Fix 3 — splitCsv:** Exportado em `user-variables.service.ts` ao final do arquivo com JSDoc explicando a diferença vs `getNegativeTermsArray` (splitCsv retorna `string[] | undefined`, `getNegativeTermsArray` retorna sempre `string[]`). `getNegativeTermsArray` foi deixado intocado — a API diverge propositalmente (semântica "sem valor" vs "array vazio") e refatorar introduziria risco sem ganho. Na route, removido o helper local e trocado por `import { ..., splitCsv } from "@/lib/wizard-services/user-variables.service"`.

**Verificação:** `tsc --noEmit --skipLibCheck` zero erros nos três arquivos; `npm run test` = 30/30 passando (3 test files).</result>
<usage><total_tokens>55932</total_tokens><tool_uses>11</tool_uses><duration_ms>86965</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a96af71efa1b3aae8</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-jonassilva-Documents-developer-maquina-de-conteudo/3877d24a-7307-489b-9482-0f0573432eda/tasks/a96af71efa1b3aae8.output</output-file>
<status>completed</status>
<summary>Agent "PR2-FIX-C: injection.test.ts" completed</summary>
<result>All 66 tests pass (36 new for injection + 30 existing).

---

## Relatório

**Arquivo criado**: `/Users/jonassilva/Documents/developer/maquina-de-conteudo/src/lib/brands/__tests__/injection.test.ts`

**Tests**: 36 tests novos, **todos passam**. Total do suite agora: 66/66 passando (4 arquivos).

**Cobertura por função** (`describe` blocks):
- `brandConfigToPromptVariables` (2) — todas undefined quando marca não configurada + regressão dos defaults do schema
- `buildTone` (7) — threshold 59/60/61, ordem estável, casos vazios, tom sem separator órfão
- `buildTargetAudience` (4) — avatar parcial sem `—` trailing, múltiplos avatares, array vazio, todos preenchidos
- `buildAudienceFears` (3) — separator `", "`, filtragem, undefined
- `buildAudienceDesires` (3) — separator `", "`, filtragem de espaços, undefined
- `buildPreferredCTAs` (3) — undefined para CTA vazia, concat `" | "`, sem pilares
- `buildDifferentiators` (4) — sem `" ∥ "` órfão em estruturas parciais, combo completo
- `buildBrandVoice`, `buildNiche`, `buildNegativeTerms`, `buildContentGoals` (2 each) — contratos básicos

**Ajuste necessário (1 teste)**: A spec do prompt dizia que `createEmptyBrandConfig()` deveria produzir todas 10 chaves `undefined`. **Isso é falso**: o `brandVoiceAtributosSchema` no schema tem defaults oficiais (80/70/75/75/30) que disparam `buildTone`. Corrigi zerando `voice.atributos` explicitamente para representar uma marca realmente não-configurada, e adicionei um segundo teste de regressão documentando o comportamento dos defaults nativos.

**Race condition**: não detectada — o arquivo `injection.ts` já estava no estado pós-refactor do Agent A (separator `", "`, `joinIfAny` exportado, `TOM_ATRIBUTO_THRESHOLD = 60` constante, `mergeBrandWithUserVars` inexistente). Todos os testes refletem essa realidade.

**Mocks necessários**: `vi.mock("server-only", () =&gt; ({}))` e `vi.mock("../context", ...)` para evitar imports do `@/db` via `context.ts`.</result>
<usage><total_tokens>55927</total_tokens><tool_uses>25</tool_uses><duration_ms>171279</duration_ms></usage>
</task-notification>

---

sim, prossiga. 3

---

<task-notification>
<task-id>aaa53d2158a147428</task-id>
<tool-use-id>toolu_01D1UFBaoHFcDuaUESNq9ynL</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-jonassilva-Documents-developer-maquina-de-conteudo/3877d24a-7307-489b-9482-0f0573432eda/tasks/aaa53d2158a147428.output</output-file>
<status>completed</status>
<summary>Agent "PR3-D: versions dialog" completed</summary>
<result>Typecheck passed (no errors grep'd for the file).

---

**Report**

- **Arquivo criado:** `/Users/jonassilva/Documents/developer/maquina-de-conteudo/src/app/(app)/settings/components/sections/brand/brand-versions-dialog.tsx`
- **Linhas:** ~195
- **Typecheck:** `npx tsc --noEmit --skipLibCheck` passou sem erros para o arquivo.

**Escolha de UX para confirmação:** Optei por `window.confirm()` simples (conforme instrução explícita), em vez de um sub-dialog aninhado. Racional: (1) evita gerenciar estado de sub-dialog dentro de um dialog já aberto (problemas conhecidos de foco/z-index no Radix), (2) a mensagem "Restaurar esta versão? A versão atual será preservada como snapshot." tranquiliza o usuário (ação reversível), reduzindo a necessidade de fricção extra, (3) mantém escopo do arquivo enxuto.

**Pequenos toques UX:**
- `restoringId` bloqueia outros botões enquanto um restore está em andamento (evita race conditions).
- Reset de estado ao fechar o dialog (evita flash de versões antigas no próximo open).
- `try/catch` em `new Date().toLocaleString("pt-BR")` por segurança (fallback para string bruta).
- Botão "Atualizar" no header, desabilitado durante loading.
- Empty state distinto (ícone Clock + mensagem amigável) vs loading state (spinner).
- Tema dark consistente com design system (`bg-[#1a1a2e]`, `border-white/10`, `text-white/*`, `text-primary`).</result>
<usage><total_tokens>36994</total_tokens><tool_uses>6</tool_uses><duration_ms>65842</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>afabe238b51324d30</task-id>
<tool-use-id>toolu_01DHREBizgv1bWtSG5CG15Xc</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-jonassilva-Documents-developer-maquina-de-conteudo/3877d24a-7307-489b-9482-0f0573432eda/tasks/afabe238b51324d30.output</output-file>
<status>completed</status>
<summary>Agent "PR3-B: forms simples" completed</summary>
<result>## Report

Criados 3 forms client-side em `/Users/jonassilva/Documents/developer/maquina-de-conteudo/src/app/(app)/settings/components/sections/brand/`:

- `brand-identity-form.tsx` — 283 linhas
- `brand-voice-form.tsx` — 392 linhas
- `brand-visual-form.tsx` — 277 linhas

**Typecheck:** `npx tsc --noEmit --skipLibCheck` passou sem erros nos 3 arquivos.

**Decisões de UI:**
- **Slider (voice):** usei `&lt;input type="range" min=0 max=100 step=1&gt;` nativo com `accent-primary` (Tailwind) — não há Slider em `@/components/ui`. Valor atual mostrado em chip `bg-white/10` monoespaçado à direita do label. Cada atributo tem hint curto ("— vai ao ponto, sem rodeios") para contexto.
- **LogoPreview (visual):** subcomponente interno com fallback — placeholder `ImageOff` quando URL vazio, erro vermelho quando `onError` dispara. Reseta erro via `useEffect` quando URL muda. Usei `&lt;img&gt;` nativo (com eslint-disable) pois as URLs são externas/arbitrárias.
- **Visual read-only:** tokens (`colors`, `fonts`, `spacing`, `shadows`) renderizados a partir de `Object.entries`. Cores mostram swatch + nome + hex; fontes renderizam o nome na própria família. Payload do save preserva `tokens` inalterado.
- **Padrão compartilhado:** `useState` local sincronizado com `brand.config.*` via `useEffect` (para refresh externo). Botão Salvar desabilita durante `isSaving`, mostra `Loader2` spinning. Seções envolvidas em `rounded-lg border border-white/[0.05] bg-white/[0.02]`. Removers usam `Trash2` ghost button que vira vermelho no hover.
- **Listas de strings** (beliefs, crencasCombatidas, antiPatterns, vocab.use/avoid) têm vazio-state (`text-white/40`) e botão "+ Adicionar" no header da seção.

Nenhum commit feito. Nenhum arquivo fora do escopo tocado.</result>
<usage><total_tokens>56359</total_tokens><tool_uses>18</tool_uses><duration_ms>173744</duration_ms></usage>
</task-notification>