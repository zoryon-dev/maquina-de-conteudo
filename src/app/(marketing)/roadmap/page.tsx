import { Map } from "lucide-react"
import { PagePlaceholder } from "@/components/page-placeholder"

export default function RoadmapPage() {
  return (
    <PagePlaceholder
      title="Roadmap"
      description="Veja o que estamos construindo e o que vem a seguir. Em breve."
      icon={Map}
    />
  )
}
