import { Mail } from "lucide-react"
import { PagePlaceholder } from "@/components/page-placeholder"

export default function ContactPage() {
  return (
    <PagePlaceholder
      title="Contato"
      description="Em breve teremos um canal oficial para suporte e vendas."
      icon={Mail}
    />
  )
}
