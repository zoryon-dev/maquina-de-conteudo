# ═══════════════════════════════════════════════════════════════════════════════
# MÁQUINA DE CONTEÚDO - TEMPLATE DE VARIÁVEIS DE AMBIENTE
# ═══════════════════════════════════════════════════════════════════════════════
# Copie este arquivo para .env.local e preencha com suas chaves de API.
#
# 📌 OBRIGATÓRIOS para funcionamento básico:
#    • DATABASE_URL              - Neon PostgreSQL connection string
#    • ENCRYPTION_KEY            - Chave para criptografia (openssl rand -base64 32)
#    • CLERK_SECRET_KEY          - Autenticação Clerk
#    • OPENROUTER_API_KEY        - LLMs via OpenRouter
#    • VOYAGE_API_KEY            - Embeddings para RAG
#    • UPSTASH_REDIS_REST_URL   - Fila para jobs em background
#    • UPSTASH_REDIS_REST_TOKEN - Token do Upstash
#
# 📌 OPCIONAIS:
#    • FIRECRAWL_API_KEY         - Web scraping
#    • TAVILY_API_KEY            - Busca web em tempo real
# ═══════════════════════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────────────────────
# DATABASE (Neon PostgreSQL)
# ─────────────────────────────────────────────────────────────────────────────
# Obtenha em: https://console.neon.tech/
DATABASE_URL=postgresql://user:password@host/database

# ─────────────────────────────────────────────────────────────────────────────
# ENCRYPTION
# ─────────────────────────────────────────────────────────────────────────────
# Gerar uma chave segura: openssl rand -base64 32
ENCRYPTION_KEY=base64-encoded-key-here

# ─────────────────────────────────────────────────────────────────────────────
# CLERK AUTHENTICATION
# ─────────────────────────────────────────────────────────────────────────────
# Obtenha em: https://dashboard.clerk.com/
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_
CLERK_SECRET_KEY=sk_test_
CLERK_WEBHOOK_SECRET=whsec_
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# ─────────────────────────────────────────────────────────────────────────────
# QUEUE (Upstash Redis)
# ─────────────────────────────────────────────────────────────────────────────
# Obtenha em: https://upstash.com/
UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=xxx

# ─────────────────────────────────────────────────────────────────────────────
# 🤖 AI SERVICES (Controladas pelo sistema)
# ─────────────────────────────────────────────────────────────────────────────
# OpenRouter - Multi-LLM aggregator (OBRIGATÓRIO)
# Obtenha em: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-

# Voyage AI - Embeddings para RAG (OBRIGATÓRIO)
# Obtenha em: https://dash.voyageai.com/api-keys
VOYAGE_API_KEY=voyage-

# Firecrawl - Web scraping (OPCIONAL)
# Obtenha em: https://www.firecrawl.dev/account
FIRECRAWL_API_KEY=fc-

# Tavily - Busca web (OPCIONAL)
# Obtenha em: https://tavily.com/account
TAVILY_API_KEY=tvly-

# Zep Cloud - Context engineering e memória multi-agent (OBRIGATÓRIO)
# Obtenha em: https://app.getzep.com/
ZEP_API_KEY=sk_

# ─────────────────────────────────────────────────────────────────────────────
# ⚙️ MODEL CONFIGURATION
# ─────────────────────────────────────────────────────────────────────────────
# Modelos padrão (opcional)
DEFAULT_TEXT_MODEL=openai/gpt-4.1
DEFAULT_IMAGE_MODEL=openai/gpt-5-image

# ─────────────────────────────────────────────────────────────────────────────
# 🌐 APP CONFIGURATION
# ─────────────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=contentMachine

# ─────────────────────────────────────────────────────────────────────────────
# 🔒 WORKER SECRET
# ─────────────────────────────────────────────────────────────────────────────
# Em produção, use uma chave forte
WORKER_SECRET=dev-secret-change-in-production
# CRON_SECRET é usado para autenticar requisições de cron (QStash, Vercel, etc.)
CRON_SECRET=dev-cron-secret-change-in-production

# ─────────────────────────────────────────────────────────────────────────────
# ⏰ UPSTASH QSTASH (Cron Jobs alternativo ao Vercel Cron)
# ─────────────────────────────────────────────────────────────────────────────
# O QStash permite executar cron jobs gratuitamente sem depender do plano Pro da Vercel.
# Obtenha em: https://console.upstash.com/qstash
#
# IMPORTANTE: Você pode usar o mesmo token do UPSTASH_REDIS_REST_TOKEN,
# ou criar um token específico para QStash.
#
# Setup após configurar:
#   npm run cron:setup   # Configura os cron jobs no QStash
#   npm run cron:health  # Verifica se os crons estão ativos
#   npm run cron:remove  # Remove todos os cron jobs
#
# QSTASH_TOKEN - Token de autenticação do QStash (opcional, usa UPSTASH_REDIS_REST_TOKEN se não definido)
QSTASH_TOKEN=
#
# QSTASH_SIGNING_KEY - Chave para verificar assinaturas das requisições do QStash (opcional)
# Geralmente é a mesma que QSTASH_TOKEN ou UPSTASH_REDIS_REST_TOKEN
QSTASH_SIGNING_KEY=
#
# QSTASH_BASE_URL - URL base da API QStash (geralmente não precisa alterar)
# QSTASH_BASE_URL=https://qstash.upstash.io

# ─────────────────────────────────────────────────────────────────────────────
# 📦 CLOUDFLARE R2 STORAGE
# ─────────────────────────────────────────────────────────────────────────────
# Para armazenamento de arquivos em cloud (documentos, imagens, etc.)
# Obtenha em: https://dash.cloudflare.com/ -> R2 -> Overview -> R2 API Tokens
#
# Provider: "local" | "r2" (default: local)
STORAGE_PROVIDER=local
#
# R2 Credentials (necessário apenas se STORAGE_PROVIDER=r2)
R2_ACCOUNT_ID=your-account-id
R2_ACCESS_KEY_ID=your-access-key-id
R2_SECRET_ACCESS_KEY=your-secret-access-key
R2_BUCKET_NAME=maquina-de-conteudo
R2_PUBLIC_URL=https://pub-xxx.r2.dev
# Domínio personalizado para arquivos públicos (opcional)
R2_CUSTOM_DOMAIN=storage-mc.zoryon.org
# Endpoint S3 da Cloudflare R2 (geralmente não precisa alterar)
R2_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
#
# Local Storage (fallback se R2 não configurado)
LOCAL_UPLOAD_DIR=public/uploads/documents

# ─────────────────────────────────────────────────────────────────────────────
# 🔗 META (Instagram & Facebook) OAuth
# ─────────────────────────────────────────────────────────────────────────────
# Obtenha em: https://developers.facebook.com/apps/
#
# Guia completo de configuração: temporario/meta-fallback-url-guide.md
#
# META_REDIRECT_URI deve ser configurado EXATAMENTE igual no Meta App Console:
# - Desenvolvimento: http://localhost:3000/api/social/callback
# - Produção: https://yourdomain.com/api/social/callback
#
META_APP_ID=your_meta_app_id
META_APP_SECRET=your_meta_app_secret
META_REDIRECT_URI=http://localhost:3000/api/social/callback

# ─────────────────────────────────────────────────────────────────────────────
# 🖼️ IMAGE GENERATION (Wizard)
# ─────────────────────────────────────────────────────────────────────────────
# ScreenshotOne - HTML to Image rendering (OPCIONAL)
# Obtenha em: https://dash.screenshotone.com/
#
# • Use o ACCESS KEY (não o Secret Key) para autenticação padrão
# • O Secret Key é opcional, apenas para assinar URLs públicas
#
# Para gerar imagens com templates HTML (fallback quando Gemini/Freepik não estão disponíveis)
SCREENSHOT_ONE_ACCESS_KEY=your-access-key-here
# SCREENSHOT_ONE_SECRET_KEY=your-secret-key-here  # Opcional - apenas para URLs públicas assinadas
