Step 1: Get file structure and read `package.json` and `tsconfig.json` to understand the tech stack.
Step 2: Look at a component in `src/components/ui` to see the styling and structure.
Step 3: Look at a page in `src/app` to see how pages are constructed.
Step 4: Check if there are any specific internal libraries or utils.

Let's begin with Step
